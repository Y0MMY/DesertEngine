# Программа T — «Виды облаков»: форма как данные, тип как ассет

Статус: **ЗАКРЫТА 2026-08-19.** Все четыре фазы сданы и слиты в `dev` (`cce28339`).
Утверждена тем же днём (решения D-10 … D-15 в `ANALYSIS_APPROACH.md` §7).
Автор: тимлид-архитектор.

## Итог программы

| фаза | что стало возможно | чем доказано |
|---|---|---|
| **T2** | том шума — файл `.dcnv`, который художник печёт панелью и подставляет в слот | один вид облака с двумя томами даёт разный характер кромки |
| **T0** | профиль высоты — порождаемая таблица, а не одна зашитая кривая; оболочка слоя вычисляется из вида | три вида различимы и глазом, и числами: на середине stratus 0.144, congestus 0.491, cumulonimbus 0.327 |
| **T1** | вид облака — файл `.decloudtype`, который художник создаёт и правит; библиотека из девяти | одна сцена, девять кадров, между кадрами меняется только ассет в слоте |
| **T3** | у каждого вида своё поле размещения со своим масштабом и анизотропией; 2–4 вида в одном небе | stratocumulus и congestus стоят рядом и друг за другом; 3096 из 9216 колонок несут оба вида |

**Шесть точек протокола не просели ни в одной фазе после T0** — совпадение до третьего знака.

Три ограничения, которые фазы называли и откладывали, T3 сняла с кадрами и числами: cirrus стал
волокнистым (ячейка 1.05 км поперёк, 8.4 км вдоль), lenticular и altocumulus разошлись **по роду**
(растяжение поперёк потока против вдоль — один контроль по разные стороны единицы), stratocumulus в
зените стал валами с синевой между ними (контраст 0.474 → 0.542).

## Что программа НЕ дала, названо честно

- **Колода отдельных подушек.** Доли Alligator не сливаются по построению: `best − second` кладёт
  ноль между каждой парой ячеек. Ни профиль, ни веса, ни несколько полей размещения этого не
  меняют — измерено трижды. Лечит второй производитель шва (§4.2 анализа): авторские воксельные
  тома, фаза Э4.
- ~~**Один том шума на весь слой**, из первого слота: cirrus рядом с cumulus эродируется томом
  кумулуса.~~ **ЗАКРЫТО 2026-08-27, фаза NV** (`CALIBRATION.md` §NV). Том вида доходит до марша из
  любого слота: четыре `sampler3D` на проход вместо одного, схлопнутые на CPU, `vec4 SpeciesNoise` в
  блоке. **Формулировка выше была неточна и это стоило программе долга:** брался не «первый слот», а
  первый НЕПУСТОЙ, поэтому художник с цирусом во втором слоте и пустым первым видел свой том и делал
  вывод, что всё работает. Дефект показан кадром до починки — 0 расходящихся пикселей из 980 480 во
  всех шести точках протокола при цирусе во втором слоте против 50.5–72.6 % при цирусе в первом.
- **Сыпь на тонких структурах.** Отношение «шаг марша против структуры» T2 привёл к 1.03 по
  Найквисту, но T0 сделал оболочку зависящей от вида, а T3 добавил масштаб размещения на вид —
  аргумент этого отношения сменился дважды под ним же. Переизмеряется по видам.

---

Является планом работ (исполнен).
Автор: тимлид-архитектор. Работает поверх `DEV_CONTRACT.md` — контракт этим документом не отменяется
и не смягчается.

---

## 0. Что попросил владелец и как это переведено в работу

Дословный запрос: **инструмент художника, чтобы создавать разные виды/типы облаков, и потом
загружать их в свойство материала облаков (текстуры шума, как в UE), чтобы были свои виды-типы.**

Перевод в инженерные термины, и он не буквальный — расхождение называю сразу:

| В запросе | Что делаем | Почему не буквально |
|---|---|---|
| «свойство материала облаков» | **ассет данных**, а не узел графа | D-5 остаётся в силе: редактор материалов для облаков не строится (§4.6 анализа). Но D-5 запрещал именно ГРАФ. Инстанс-параметры и текстуры шума — то, что в UE реально настраивает художник, — не запрещены и делаются здесь |
| «текстуры шума, как в UE» | **том шума становится ассетом с генератором**, фаза T2 | сегодня шум не текстура, а результат GPU-бейка по seed/octaves; чтобы его можно было «загрузить в свойство», он обязан сначала стать файлом |
| «разные виды-типы облаков» | ассет вида + **набор видов, смешиваемых по небу** (T3) | один ассет в слоте даёт «одно небо одного вида». Это не то, что просили |

---

## 1. Предупреждение, которое определяет порядок фаз

**Сегодняшняя модель формы физически не способна различить виды облаков.**

Факты из дерева на 2026-08-19:

- `CloudField.glslh:80` — `CloudVerticalProfile( heightFraction, cloudType )`: одна кривая с двумя
  зашитыми числами, `topEnd = mix(0.06, 0.35)`, `baseEnd = mix(0.01, 0.05)`.
- `CloudField.glslh:130` — анизотропия зашита константой `2.2`.
- Характер эрозии один: два канала тома, выбираемые скаляром `DetailType`.
- Всё это управляется **одним скаляром** `CloudType` на компоненте.

Из этого следует: библиотека из девяти именованных типов, построенная поверх этой формы, даст
девять подписей к одному облаку. Ползунок, который ничего не двигает, — TODO, замаскированный под
фичу (§1.3 контракта); ассет, который ничего не двигает, — то же самое, только на порядок дороже,
потому что он ещё и уезжает в сцены и в миграцию.

**Поэтому T0 идёт первым и его критерий выхода — не код, а три различимых кадра.**

---

## 2. Фазы

### T0. Форма, различающая виды

Профиль высоты, анизотропия и характер эрозии перестают быть константами и становятся входом.

**Модель.** В `Common/CloudField.glslh` появляется `CloudShapeParams` — то, что позже целиком
переезжает в ассет:

```
BaseAltitudeKm, TopAltitudeKm      абсолютные высоты вида
BaseSharpness, TopTaper            форма рампы у основания и длина таяния верхушки
Anisotropy                         отношение горизонтальной частоты к вертикальной
CoverageTileKm, CoverageContrast   поле покрытия
DetailTileKm, DetailStrength, DetailCharacter   эрозия: масштаб, глубина, wispy↔billowy
DensityScale, ExtinctionScale      сколько вещества в этом виде
```

**Ключевое решение фазы: высоты вида — АБСОЛЮТНЫЕ, а не доли оболочки.** Сегодня профиль занимает
долю слоя, и это значит, что подъём `LayerBottomAltitude` тащит за собой все виды разом. Вид,
который говорит «моё основание на 1.2 км, верхушка на 2.6 км», — это правило §5.1 анализа
(абсолютный якорь высоты) в самой сильной форме: набор одних лишь отношений удовлетворяется при
любом абсолютном масштабе, и никакой тест не покраснеет на слое, уехавшем на километры.

**Следствие, за которым надо следить.** Оболочка компонента (`LayerBottomAltitude`,
`LayerThickness`) и высоты видов — две величины, обязанные согласовываться, то есть ровно тот класс
дефектов из §2.3.1 контракта. Правило: **оболочка вычисляется, а не авторится** — она есть
объединение диапазонов всех активных видов, считается один раз в `PackCloudParams` и покрыта тестом
на отношение `envelope ⊇ каждый активный вид`. Руками её никто не выставляет.

**Тесты (сюита `CloudField`, двойная компиляция):**
- профиль равен нулю строго вне `[BaseAltitudeKm, TopAltitudeKm]`;
- метеорологический якорь: stratus не поднимается выше 600 м, cumulus mediocris лежит в 0.8–2.0 км,
  cumulonimbus начинается в 0.5–1.5 км;
- три вида дают три разных интеграла профиля (иначе они не различимы по построению);
- оболочка содержит каждый активный вид.

**⬛ ПОКАЗ T0:** stratus, cumulus congestus, cumulonimbus — по три кадра на вид (зенит `0,0.9,-1`,
середина `0,0.45,-1`, горизонт `0,0.12,-1`), плюс числа `Tools/ImageStat` по каждому. Три вида
обязаны отличаться и глазом, и числами.

**Владение файлами:** `Common/CloudField.glslh`, `Common/CloudParams.glslh`,
`Graphic/Clouds/CloudPayload.hpp`, `ECS/VolumetricCloudComponent.hpp`, сюита `CloudField`.

---

### T1. Ассет «Тип облака»

Параметры формы уезжают из компонента в именованный ассет.

**Что делается:**
- `Engine/Assets/CloudTypeAsset.*` рядом с `MaterialAsset` — тот же `AssetBase`/`AssetManager`, та же
  сериализация, тот же Content Browser, тот же DnD, тот же хот-релоад. Мы не заводим вторую систему
  ассетов (§2.2 контракта).
- В компоненте поля формы **удаляются** и заменяются одним хендлом. Не «deprecated, но читаем», не
  флаг — §4 контракта, старый путь сносится той же правкой.
- **Миграция**: загрузчик поднимает версию сцены, собирает из инлайновых значений сцены ассет,
  кладёт его рядом со сценой, подставляет хендл и **пишет об этом в лог с именами и числами**
  (§4.7 — молчаливая миграция запрещена). Функция миграции чистая и покрыта тестом.
- Сцены репозитория пересохраняются в рамках этой же задачи (§4.5).
- Библиотека видов по каталогу из анализа §6: cumulus humilis / mediocris / congestus,
  cumulonimbus, stratocumulus, stratus, altocumulus, cirrus, орографическое (линзовидное).

**⬛ ПОКАЗ T1:** одна сцена, девять кадров, между кадрами меняется **только** ассет в слоте.

**Владение файлами:** `Engine/Assets/CloudType*`, сериализация ассета, панель свойств компонента,
`Editor/Resources/Assets/Clouds/*`, сцены, тест миграции.

---

### T2. Том шума как ассет и его генератор

Это и есть «текстуры шума, как в UE».

**Что делается:**
- Каналы тома получают смысл из доклада (сл. 122) вместо четырёх безымянных октав Perlin:
  Curly-Alligator LF/HF и Alligator LF/HF. 128³ RGBA8 = 8.4 МБ, в бюджет D-9 (64 МБ) укладывается.
- `CloudNoiseVolumeAsset` + собственный контейнер с заголовком (размеры, формат, смысл каналов,
  seed, версия генератора). **BCn/DDS/KTX не берём** — в дереве нет ни одного из них, а на Apple
  Silicon BC для 3D-текстур не экспонируется (§2.2 анализа).
- Панель редактора «Cloud Noise Volume»: параметры, превью срезов, бейк, сохранение в ассет.
- **Бейк делается на CPU, и это не компромисс, а следствие уже принятого приёма.** У `Image3D` нет
  CPU-readback (§2.3 анализа), поэтому сохранить результат GPU-бейка нечем. Но шумовые функции уже
  компилируются как C++ ради тестов — тот же самый код порождает файл офлайн, и совпадение с
  шейдерной версией гарантировано механизмом двойной компиляции, а не проверяется. 8.4 млн отсчётов
  офлайн — секунды.
- `CloudNoiseBake.shader` и путь GPU-бейка **удаляются**. Один источник тома, ровно один (§4.2).

**⬛ ПОКАЗ T2:** один и тот же вид облака с двумя разными том-ассетами — разный характер кромки.

**Владение файлами:** `Engine/Assets/CloudNoiseVolume*`, генератор, панель, `CloudNoiseBake.shader`
(на удаление), `VolumetricCloudRenderer.cpp` в части загрузки тома.

---

### T3. Несколько видов на одном небе

**Что делается:**
- Компонент держит **набор** из 2–4 хендлов вида плюс поле типа (низкочастотный шум, уже
  считываемый как `noise.y` — обобщение нынешнего `CloudTypeVariance`).
- **Правило смешения — не lerp параметров.** Интерполяция между stratus и cumulonimbus не даёт ни
  того, ни другого: она даёт среднее, которого в природе нет, и именно так библиотека видов
  превращается обратно в один вид. Берётся то же правило, что и для двух производителей в §4.3
  анализа: `Profile = max по видам ( weight_i × profile_i )`, веса — разбиение поля типа
  smoothstep'ом с узкой полосой перехода; `DetailType`, `DensityScale` и `ExtinctionScale` берутся
  от вида-победителя, иначе на стыке поплывёт характер детали.
- Стоимость ограничена: вид считается, только если его вес больше нуля И высота отсчёта попала в его
  диапазон. В типичной точке это один вид.

**⬛ ПОКАЗ T3:** один кадр, где низкий stratocumulus и башня congestus стоят в одном небе.

**Владение файлами:** `Common/CloudField.glslh`, `CloudPayload.hpp`, компонент, панель свойств.

#### T3 СДАНА (2026-08-19) — что вышло и чем это померено

Реализовано по D-14, а не по §2: **вес вида — не вес**. У каждого вида своё поле размещения (свой
масштаб, своя анизотропия, своя реализация шума), объединение — `max`, `DetailType`/`DensityScale`/
`ExtinctionScale`/`DetailFactor` берутся от вида-победителя.

- **Компонент** несёт `CloudType1..4`; миграция сцен **v5 → v6** переименовывает `CloudType` →
  `CloudType1`, сцены репозитория пересохранены (11 файлов).
- **Таблица профилей** — один канал RGBA на вид, ось высоты — **объединение** диапазонов набора.
  Потолок в четыре вида стал шириной тексела, а не вкусом.
- **Где берётся поле размещения:** та же пара каналов того же тома (Alligator LF/HF), но со своим
  масштабом, своей анизотропией и своим смещением. Обоснование — число: квантильное отображение
  откалибровано под ОДНО распределение (полуширина 0.32), поэтому вид, читающий другую пару каналов,
  ответил бы на ту же `Coverage` другой долей неба. Замер: p25/p50/p75 у четырёх видов с масштабами
  0.35 / 1.0 / 2.5 / 0.8 и растяжениями 1.6 / 1.0 / 8.0 / 0.2 расходятся не более чем на 0.056.
- **Размещение переехало в ассет вида** (`PlacementScale`, `PlacementAnisotropy`), формат
  `.decloudtype` поднят до версии 2; девять файлов библиотеки переписаны, версия 1 отвергается с
  сообщением про версию.

**Кадры.** Шесть точек протокола на `Clouds_Demo` — до и после **совпали до третьего знака**:
зенит от солнца 0.483 / на солнце 0.497, середина 0.491 / 0.385, горизонт 0.307 / 0.367. Это и есть
доказательство, что набор из одного вида — это ровно то небо, что было.

**Три названных ограничения:** cirrus и stratocumulus сняты кадрами, lenticular vs altocumulus —
сняты по роду (растяжение поперёк потока против растяжения вдоль). Подробности и остаток — в отчёте
по задаче.

---

### T-ACES (параллельно, другой владелец файлов)

Решение D-10: **ACES становится оператором по умолчанию**, Reinhard остаётся выбираемым.

Несущий вход решения: заявленная цель программы — паритет с UE, а эталон
`UEReference/UE_mid.png` снят через ACES. Пока операторы разные, любое измерение по эталону меряет
разницу тонмапперов, а не облаков: остаток расхождения — контраст 0.333 против 0.482
(`CALIBRATION.md`).

**Это не «две ветки на всякий случай».** Оператор становится выбираемым свойством сцены
(перечисление в `SceneSettings`), обе ветки — живые фичи с UI, а не путь совместимости. Тест на
отношение: значение по умолчанию у перечисления есть ACES, и сцены репозитория несут именно его.

**Объём:** `Engine/Core/SceneSettings.hpp`, `Programs/Composite/SceneComposite.shader`,
`Materials/PostProcessing/MaterialTonemap.*`, пересохранение сцен, перезамер по `Tools/ImageStat` и
обновление `CALIBRATION.md` новыми числами.

**⬛ ПОКАЗ:** старая и новая калибровочная таблица рядом, на одних и тех же кадрах.

---

## 3. Порядок и параллельность

Файлы разведены по §5 контракта:

| Очередь | Задача | Пересечения |
|---|---|---|
| 1 | **T0** | `CloudField.glslh`, компонент, payload |
| 1, параллельно | **T-ACES** | пост-обработка и сцены — с облаками не пересекается ни одним файлом |
| 2 | **T1** | слой ассетов, панель, миграция |
| 3 | **T2** | генератор и загрузка тома |
| 3, параллельно | **остаток Э3**: тиры качества, тени облаков на мир | рендерер и новый теневой проход; стартует после T1, потому что до этого делит `CloudPayload.hpp` с T0 |
| 4 | **T3** | смешение видов |
| 5 | **Э4** | лепка конкретных облаков — принимает от T2 готовый формат объёмного ассета и загрузчик |

---

## 3а. РЕВИЗИЯ ПОСЛЕ РАЗБОРА ГРАФА UE (2026-08-19)

Исследование R1 (`UEReference/Documentation/ShapeModel.md`) опровергло две конструкции, которые я
заложил в §2 выше. Записываю это здесь, а не переписываю §2 молча: план, у которого не видно, где он
ошибался, невозможно ни оспорить, ни проверить.

### Опровергнуто: профиль как параметрическая кривая (T0)

Я написал: «BaseAltitudeKm, TopAltitudeKm, BaseSharpness, TopTaper». У UE профиль — **двумерная
таблица** `f(нормированная высота в слое, значение паттерна размещения)`, канал на вид: у края пятна
вид низкий и плоский, в центре того же пятна — башня. Параметрическая кривая этого не выражает
принципиально, как не выражает и наковальню кумулонимбуса — она многомодальна по высоте.

**Решение (D-13). Рантайм-представление профиля — двумерная таблица, как у UE. Параметрическая кривая
остаётся, но как ГЕНЕРАТОР таблицы, а не как форма хранения.**

Это не уступка эталону, а то, что делает T0 и T1 одной работой вместо двух: «ассет типа облака» и
есть таблица профилей плюс параметры плюс ссылка на том шума, а инструмент художника — то, чем эта
таблица порождается и правится. Сцена без художника не пустеет: генератор строит таблицу из
параметров вида, и умолчание работает из коробки.

Параметрическая работа T0 при этом не выбрасывается — она переезжает на вход генератора.

### Опровергнуто: смешение видов разбиением одного поля (T3)

Я написал: «веса — разбиение поля типа smoothstep'ом». У UE вес вида — **независимое двумерное поле
на вид**: свой канал, свой масштаб. Разница содержательная и бьёт ровно в критерий приёмки: разбиение
одного поля означает, что сумма весов равна единице, то есть **виды не могут перекрываться** — а
кадр, заявленный в ⬛ ПОКАЗ T3, это кадр, где низкий stratocumulus и башня congestus стоят рядом и
местами друг за другом. Я записал критерий, который моё же правило смешения делает недостижимым.

**Решение (D-14). Вес вида — независимое поле на вид. Объединение остаётся `max` (оно подтверждено
эталоном), но веса перестают быть разбиением.**

### Добавлено в объём T0: домен-варп по высоте

Механизм, которым UE борется с вертикальной экструзией, — не профиль, а **смещение координат базового
шума вторым шумом на вектор, зависящий от высоты в слое**. У нас этого нет вовсе; мы решали ту же
задачу иначе — полностью трёхмерной выборкой поля покрытия.

Оба решения законны, и второе у нас уже работает. Поэтому варп входит в T0 **как измеряемая добавка**,
а не как замена: разработчик показывает кадры до и после и числа `ImageStat`. Если добавки не видно —
он пишет это в отчёте, и варп не берётся.

Источник подтверждён дважды и независимо: R1 вывел варп трассировкой узлов графа, а официальная
документация Epic описывает `Noise2_Coordinates` как «controls **distortion** amount and scale for
Noise1» (`UEReference/Documentation/EpicDoc_CloudMaterial.md` §4). Требование кадра остаётся, но
относится теперь к нашей реализации, а не к самому факту.

### Подтверждено первоисточником: сколько видов живёт в одном небе

Документация Epic закрепляет четыре вида за каналами RGBA **трёх** текстур раскладки — профиль,
размещение, маска: R Stratocumulus, G Altostratus, B Cirrostratus, A Nimbostratus.

Отсюда две правки к §2:

- **«2–4 вида» в T3 получает причину вместо вкуса**: потолок структурный, это каналы RGBA. Библиотека
  на диске может быть любого размера — ограничение на число видов в ОДНОМ небе.
- **Масштаб размещения свой у каждого вида** (`Layout_CloudPerTypeScale`). У нас один тайл погоды на
  весь слой; поле на вид (D-14) тянет за собой и масштаб на вид. Входит в T3.

Каталог из девяти видов в §2 (T1) при этом остаётся: он описывает библиотеку на диске, а не то, что
одновременно в кадре.

### Сохранено вопреки эталону: порог покрытия

У UE покрытие — аддитивный биас внутри шума, без клампа и без порога; ни `remap`, ни `smoothstep` в
графе нет. У нас — `smoothstep` вокруг порога.

**Решение (D-15): не трогаем.** Несущий вход: наш порог появился как лечение измеренного дефекта —
`remap` давал медиану поля на 0.09 и небо, равномерно залитое вуалью (`CloudField.glslh:159-184`).
Композиция UE отличается целиком, поэтому перенести из неё одно звено — значит вернуть дефект в
обмен на сходство с эталоном в детали, которую никто не мерил. Пересматривается только если кадры T0
покажут, что порог борется с таблицей профилей.

## 4. Что эта программа НЕ делает

- Не строит граф материала облаков — D-5 в силе.
- Не пишет флюид-солвер: «цветную капусту» с с. 69 доклада даёт симуляция, у нас её не будет
  (зафиксировано в анализе §6).
- Не трогает воксельную лепку — это Э4, и она идёт после T2, которая готовит ей формат данных.
