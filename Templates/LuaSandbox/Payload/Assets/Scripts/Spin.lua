-- Starter script: rotates the entity it is attached to.
--
-- Attach it to an entity in the Editor (Details -> Add Component -> Script), then uncomment the
-- line below. Left commented so a fresh project runs without doing anything surprising.
function OnUpdate(dt)
    -- self.transform.rotation.y = self.transform.rotation.y + dt
end
