// Single translation unit that compiles the stb_truetype implementation (public-domain, v1.26).
// Mirrors stb_image.cpp — the header is included as a declarations-only include everywhere else.
#define STB_TRUETYPE_IMPLEMENTATION
#include "stb_truetype/stb_truetype.h"
